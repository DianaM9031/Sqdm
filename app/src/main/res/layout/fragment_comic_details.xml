<?xml version="1.0" encoding="utf-8"?>
<!-- <layout xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <import type="android.example.dianamoron.sqdm.view.util.StringUtil"/>
        <variable
            name="comic"
            type="com.example.dianamoron.sqdm.service.model.Comic"/>
    </data> -->
    <ScrollView  xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginBottom="16dp"
            android:layout_marginTop="16dp"
            android:orientation="vertical"
            android:gravity="center">

            <ImageView
                android:id="@+id/comic_image"
                android:layout_width="150dp"
                android:layout_height="150dp"
                />
            <!-- app:imageUrl="@{comic.image.small_url}" -->

            <TextView
                android:id="@+id/comic_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="@dimen/text_margin"
                android:textAppearance="@style/Base.TextAppearance.AppCompat.Large"
                />
            <!-- android:text="@{comic.name}" -->

            <TextView
                android:id="@+id/comic_description"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="@dimen/text_margin"
                android:textAppearance="@style/Base.TextAppearance.AppCompat.Large"
                />
            <!-- android:text="@{StringUtil.fromHtml(comic.description)}" -->

        </LinearLayout>

    </ScrollView>
<!-- </layout> -->


